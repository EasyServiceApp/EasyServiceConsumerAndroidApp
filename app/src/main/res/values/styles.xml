<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">

        <!-- Customize your theme here. -->
        <item name="android:spinnerItemStyle">
            @style/spinnerItemStyle
        </item>

        <!-- For each individual Spinner list item once clicked on -->
        <item name="android:spinnerDropDownItemStyle">
            @style/spinnerDropDownItemStyle
        </item>
    </style>


    <style name="spinnerItemStyle" parent="@android:style/Widget.Holo.DropDownItem.Spinner">

        <item name="android:textColor">@color/white</item>
        <item name="android:drawableRight">@drawable/ic_down_arrow</item>
        <!--<item name="android:padding">5dp</item>
        <item name="android:textSize">11sp</item>
        <item name="android:text">11sp</item>-->

    </style>

    <style name="spinnerDropDownItemStyle" parent="@android:style/Widget.Holo.DropDownItem.Spinner">

        <item name="android:textColor">#000</item>
    </style>

    <!--<style name="customRatingBar" parent="@android:style/Widget.RatingBar">
        <item name="android:indeterminateDrawable">@drawable/review_rating_bar_full</item>
        <item name="android:progressDrawable">@drawable/review_rating_bar_full</item>
        <item name="@android:id/secondaryProgress">@drawable/half_star</item>
        <item name="android:isIndicator">false</item>
    </style>-->
</resources>
